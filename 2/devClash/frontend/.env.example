# Placeholder for the future backend base URL.
# Nothing in the app reads this yet — it exists so a real backend
# can be "turned on" later by setting one value instead of rewriting
# fetch calls throughout the app.
VITE_API_BASE_URL=
