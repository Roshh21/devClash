# DevClash — Backend

Not started yet.

This folder is reserved for the upcoming Node.js/Express API (accounts, the question bank,
evaluation, matchmaking, and everything else the frontend will eventually call over HTTP/WebSocket
via `VITE_API_BASE_URL`).

Nothing here currently runs. There is no server, no database connection, and no routes.
