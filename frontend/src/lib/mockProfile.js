// Mirrors mockDashboard.js: one local file every profile section
// reads from, so Stage D can wire it to real match history later
// without touching the layout.

export const PROFILE_STATS = {
  totalMatches: 128,
  winRate: 68,
  currentStreak: 7,
  highestRating: 1523,
};

export const FAVORITE_CATEGORIES = ['Arrays', 'DP', 'System Design', 'Debugging', 'SQL'];

export const PROFILE_QUOTE = 'Consistent effort leads to higher stakes.';
